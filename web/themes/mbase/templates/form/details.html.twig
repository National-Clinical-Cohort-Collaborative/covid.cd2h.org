{#
/**
 * @file
 * Theme override for a details element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the details element.
 * - title: (optional) The title of the element, may not be set.
 * - description: (optional) The description of the element, may not be set.
 * - children: (optional) The children of the element, may not be set.
 * - value: (optional) The value of the element, may not be set.
 
 *
 * @see template_preprocess_details()
 */
#}
{%- if isgroup -%}
  <div id="{{attributes.id}}" class="tab-pane fade active">
{%- endif -%}
<details {{ attributes.addClass('panel panel-default') }}>
  {%- if title -%}
    <summary{{ summary_attributes.addClass('panel-heading') }} data-target="#{{ attributes.id }}-child">{{ title }}</summary>
  {%- endif -%}
  <div id="{{ attributes.id }}-child" class="details-wrapper panel-body">
    {%- if children -%}
      {{ children }}
    {%- endif -%}
    {%- if value -%}
      {{ value }}
    {%- endif -%}
  </div>
    {%- if description -%}
      <div class="details-description panel-footer"> <span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span> {{ description }}</div>
    {%- endif -%}
</details>
{%- if isgroup -%}
  </div>
{%- endif -%}
